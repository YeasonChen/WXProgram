
<template name="toast">
  <view class='toast_content_box' wx:if="{{isHiden}}">
    <view class='toast_content'>
      <view class='toast_content_text'>
        {{content}}
      </view>
    </view>
  </view>
</template>